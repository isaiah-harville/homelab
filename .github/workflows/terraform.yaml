---
name: Terraform (Omni)

"on":
  workflow_dispatch:
  pull_request:
    paths:
      - terraform/**
      - talos/omni/patches/**
      - .github/workflows/terraform.yaml
  push:
    branches:
      - main
    paths:
      - terraform/**
      - talos/omni/patches/**
      - .github/workflows/terraform.yaml

permissions:
  contents: read
  pull-requests: write

concurrency:
  # One Terraform run at a time — state is shared.
  group: terraform-omni
  cancel-in-progress: false

defaults:
  run:
    working-directory: terraform/omni

jobs:
  plan:
    name: Plan
    if: github.event_name == 'pull_request' || github.event_name == 'workflow_dispatch'
    # The in-cluster runner can reach Omni and SeaweedFS over the LAN.
    runs-on: homelab
    steps:
      - name: Checkout
        uses: actions/checkout@v4.2.2

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_wrapper: false

      - name: Format check
        run: terraform fmt -check -recursive

      - name: Init
        run: terraform init -input=false

      - name: Validate
        run: terraform validate

      - name: Plan
        run: terraform plan -input=false -no-color

  apply:
    name: Apply
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: homelab
    steps:
      - name: Checkout
        uses: actions/checkout@v4.2.2

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_wrapper: false

      - name: Init
        run: terraform init -input=false

      - name: Apply
        run: terraform apply -input=false -auto-approve
