---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: traefik-internal
  namespace: traefik-internal
spec:
  interval: 30m
  install:
    disableWait: true
    remediation:
      retries: 3
  upgrade:
    disableWait: true
    remediation:
      retries: 1
      remediateLastFailure: true
      strategy: rollback
  chart:
    spec:
      chart: traefik
      version: "41.0.2"
      sourceRef:
        kind: HelmRepository
        name: traefik
        namespace: flux-system
  values:
    deployment:
      replicas: 2
    podDisruptionBudget:
      enabled: true
      minAvailable: 1
    topologySpreadConstraints:
      - maxSkew: 1
        topologyKey: kubernetes.io/hostname
        whenUnsatisfiable: DoNotSchedule
        labelSelector:
          matchLabels:
            app.kubernetes.io/name: traefik
            app.kubernetes.io/instance: traefik-internal-traefik-internal
    ingressClass:
      enabled: true
      isDefaultClass: false
      name: traefik-internal
    additionalArguments:
      - "--entrypoints.web.http.redirections.entryPoint.to=:443"
      - "--entrypoints.web.http.redirections.entryPoint.scheme=https"
    providers:
      kubernetesIngress:
        ingressClass: traefik-internal
    service:
      annotations:
        metallb.io/address-pool: lab-lb-pool
        # Pin the LB IP via the MetalLB annotation. The deprecated Service
        # `loadBalancerIP` field is not rendered by the chart / honored by k8s,
        # so without this MetalLB hands out pool IPs in arbitrary order and the
        # internal/public IPs can land swapped (internal must be .251 to match
        # the *.int.harville.dev DNS record).
        metallb.io/loadBalancerIPs: 10.1.10.251
      type: LoadBalancer
