apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: traefik-public
  namespace: traefik-public
spec:
  interval: 30m
  chart:
    spec:
      chart: traefik
      version: "*"
      sourceRef:
        kind: HelmRepository
        name: traefik
        namespace: flux-system
  values:
    ingressClass:
      enabled: true
      isDefaultClass: false
      name: traefik-public
    providers:
      kubernetesIngress:
        ingressClass: traefik-public
    service:
      annotations:
        metallb.universe.tf/address-pool: lab-lb-pool
      type: LoadBalancer
      loadBalancerIP: 10.1.10.252
