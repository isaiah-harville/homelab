---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: seaweedfs
  namespace: apps
spec:
  interval: 30m
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 1
      remediateLastFailure: true
      strategy: rollback
  chart:
    spec:
      chart: seaweedfs
      version: "*"
      sourceRef:
        kind: HelmRepository
        name: seaweedfs
        namespace: flux-system
  values:
    master:
      replicas: 1
      data:
        type: persistentVolumeClaim
        size: 1Gi
        storageClass: longhorn
      logs:
        type: persistentVolumeClaim
        size: 1Gi
        storageClass: longhorn
      resources:
        requests:
          cpu: 100m
          memory: 256Mi
        limits:
          memory: 512Mi
    volume:
      replicas: 1
      dataDirs:
        - name: data1
          type: persistentVolumeClaim
          size: 50Gi
          storageClass: longhorn
          maxVolumes: 0
      resources:
        requests:
          cpu: 100m
          memory: 256Mi
        limits:
          memory: 1Gi
    filer:
      replicas: 1
      data:
        type: persistentVolumeClaim
        size: 8Gi
        storageClass: longhorn
      logs:
        type: persistentVolumeClaim
        size: 1Gi
        storageClass: longhorn
      resources:
        requests:
          cpu: 100m
          memory: 256Mi
        limits:
          memory: 512Mi
    s3:
      enabled: true
      replicas: 1
      port: 8333
      existingConfigSecret: seaweedfs-s3-config
      # Talos rejects the chart's default hostPath; disposable logs use emptyDir.
      logs:
        type: emptyDir
      createBuckets:
        - name: general
        - name: backups
      resources:
        requests:
          cpu: 50m
          memory: 128Mi
        limits:
          memory: 256Mi
