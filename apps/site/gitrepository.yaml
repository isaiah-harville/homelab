---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: site
  namespace: flux-system
spec:
  interval: 10m
  ref:
    branch: main
  secretRef:
    name: flux-system
  url: ssh://git@github.com/Verus-Datum/site.git
