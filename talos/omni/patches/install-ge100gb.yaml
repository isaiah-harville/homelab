# Install target = the single fixed disk >=100GB (excludes small USB media).
# For single-disk machines with no dedicated Longhorn spare.
---
machine:
  install:
    diskSelector:
      size: '>= 100GB'
