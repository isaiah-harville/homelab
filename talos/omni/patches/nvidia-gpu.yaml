# Load the NVIDIA kernel modules shipped by the nonfree-kmod-nvidia-production
# system extension. Apply only to machines with an NVIDIA GPU, alongside the
# nvidia-container-toolkit-production extension.
---
machine:
  kernel:
    modules:
      - name: nvidia
      - name: nvidia_uvm
      - name: nvidia_drm
      - name: nvidia_modeset
