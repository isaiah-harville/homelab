# Install target = the NVMe SSD (this machine also has a rotational HDD reserved
# for Longhorn). Selecting by type avoids installing onto the 1TB HDD.
---
machine:
  install:
    diskSelector:
      type: nvme
