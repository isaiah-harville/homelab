---
# Generic NVIDIA GPU node setup (node_gpu: true) — applies to ANY GPU agent,
# bare-metal or WSL. Installs the NVIDIA Container Toolkit so k3s detects the
# nvidia container runtime on first start and auto-creates the `nvidia`
# RuntimeClass. GPU labels + the dedicated=gpu taint are written by the config
# task in main.yml.

- name: Fetch the NVIDIA Container Toolkit GPG key
  ansible.builtin.get_url:
    url: https://nvidia.github.io/libnvidia-container/gpgkey
    dest: /tmp/nvidia-container-toolkit.asc
    mode: "0644"

- name: Install the key into a keyring
  ansible.builtin.command:
    cmd: >-
      gpg --batch --yes --dearmor
      -o /usr/share/keyrings/nvidia-container-toolkit-keyring.gpg
      /tmp/nvidia-container-toolkit.asc
    creates: /usr/share/keyrings/nvidia-container-toolkit-keyring.gpg

- name: Add the NVIDIA Container Toolkit apt repository
  ansible.builtin.get_url:
    url: https://nvidia.github.io/libnvidia-container/stable/deb/nvidia-container-toolkit.list
    dest: /etc/apt/sources.list.d/nvidia-container-toolkit.list
    mode: "0644"

- name: Point the repository at the signed keyring
  ansible.builtin.replace:
    path: /etc/apt/sources.list.d/nvidia-container-toolkit.list
    regexp: '^deb https://'
    replace: "deb [signed-by=/usr/share/keyrings/nvidia-container-toolkit-keyring.gpg] https://"

- name: Install nvidia-container-toolkit
  ansible.builtin.apt:
    name: nvidia-container-toolkit
    state: present
    update_cache: true
  notify: Restart k3s-agent
